var age = prompt("What is your age?");

// .25 allows for leap years because they happen every 4 years
var days = age * 365.25;

alert(age + " years is roughly " + days + "days!");
Console.log(age + " years is roughly " + days + "days!");
