//require grabs the package to use
var cat = require('cat-me'),
   joke = require('knock-knock-jokes');

// //to run point to folder in cmd line and type 'node app.js'
console.log(cat());
console.log(joke());
